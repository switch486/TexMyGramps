### Example token.env / local.env
# Create a file named `token.env` or `local.env` at the project root (not checked into git)
# and set your Mapbox access token there.
# Example:
MAPBOX_TOKEN=pk.your_mapbox_token_here

# For local development you can load it in your shell with:
#   source token.env
